<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_launch"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.reverside.uberfood.launch.LaunchActivity">

    <ImageView
        android:id="@+id/launch_IV"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@drawable/img_launch_splash"
        android:scaleType="centerCrop"/>

    <Button
        android:id="@+id/launch_enter_BTN"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/launch_IV"
        android:text="Enter"
        android:textColor="@color/colorPrimary"
        android:textSize="24sp"
        android:background="@drawable/btn_green"
        style="?android:borderlessButtonStyle"/>

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:src="@drawable/bar_text"
        android:background="#b1000000"
        android:padding="@dimen/default_padding"
        android:id="@+id/imageView"
        android:layout_alignParentStart="true"
        android:layout_alignParentTop="true"/>
</RelativeLayout>
